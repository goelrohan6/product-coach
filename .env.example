# Optional override for local data dir.
DATA_DIR=/Users/rohan/.coach-product-coach

# Optional explicit database path.
DB_PATH=/Users/rohan/.coach-product-coach/coach.db

# For MCP server startup.
MCP_PASSCODE=your-passcode

# Optional: env-driven LiteLLM config (takes precedence over DB-stored model settings).
LITELLM_BASE_URL=http://localhost:4000
LITELLM_API_KEY=your-litellm-key
LITELLM_MODEL=gpt-4o-mini
