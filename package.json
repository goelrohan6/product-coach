{
  "name": "executive-product-coach",
  "private": true,
  "version": "0.1.0",
  "description": "Local-first executive product coaching app with web + MCP interfaces",
  "packageManager": "pnpm@10.11.0",
  "scripts": {
    "dev": "pnpm --filter @coach/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  }
}
